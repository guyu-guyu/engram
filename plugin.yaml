name: engram
version: 0.1.0
description: "Engram — SQLite-backed structured memory store for AI agents. Entry-level writes, group containers, FTS5 search, organic Markdown export."
pip_dependencies: []
requires_env: []
